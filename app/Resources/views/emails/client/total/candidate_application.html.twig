{% extends 'emails/base_email.html.twig' %}
{% block body_html %}
    Hi {{ firstName }}
    <br><br>
    {% for job in jobs|jobTitle %}
        {% if (job.countApplicants is defined and job.countApplicants > 0) %}
            You have received {{ job.countApplicants }} applications for the {{ jobTitle }} job you posted on CAs Online.
            <br><br>
        {% endif %}
        {% if job.link is defined %}
            Please click <a href="{{ job.link }}">here</a> to view the applicant’s profile.
            <br><br>
        {% endif %}
    {% endfor %}
    Get hold of us on <a href="mailto:info@casonline.co.za">info@casonline.co.za</a> if you have any questions or feedback.
{% endblock %}
